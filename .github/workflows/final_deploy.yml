name: Deploy DemDem to Firebase Hosting

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔨 Build project
        run: npm run build
        env:
          VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_DATABASE_URL: ${{ secrets.VITE_FIREBASE_DATABASE_URL }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
          VITE_FIREBASE_MEASUREMENT_ID: ${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}

      - name: 🚀 Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          channelId: live
          projectId: evenpasssenegal
```

---

## ✅ SECRETS GITHUB À AJOUTER/VÉRIFIER

Avant de push ce fichier, assure-toi d'avoir **TOUS** ces secrets dans GitHub :
```
✅ FIREBASE_SERVICE_ACCOUNT (déjà ajouté)
✅ VITE_FIREBASE_API_KEY (déjà ajouté)
✅ VITE_FIREBASE_APP_ID (déjà ajouté)
✅ VITE_FIREBASE_AUTH_DOMAIN (déjà ajouté)
✅ VITE_FIREBASE_DATABASE_URL (déjà ajouté)
✅ VITE_FIREBASE_MESSAGING_SENDER_ID (déjà ajouté)
✅ VITE_FIREBASE_PROJECT_ID (déjà ajouté)
✅ VITE_FIREBASE_STORAGE_BUCKET (déjà ajouté)

❌ VITE_FIREBASE_MEASUREMENT_ID (À AJOUTER)
```

### Ajoute ce dernier secret :

**GitHub → Settings → Secrets → New repository secret**
```
Name: VITE_FIREBASE_MEASUREMENT_ID
Value: G-FVQTV8TMLJ
```

---

## 🚀 COMMENT L'UTILISER

### 1. Remplace le fichier YML

Copie le code ci-dessus et remplace **complètement** le contenu de :
```
.github/workflows/final_deploy.yml
