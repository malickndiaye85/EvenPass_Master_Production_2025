name: Deploy DemDem to Firebase

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔨 Build project
        run: npm run build
        env:
          VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_DATABASE_URL: ${{ secrets.VITE_FIREBASE_DATABASE_URL }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID }}
          VITE_FIREBASE_MEASUREMENT_ID: ${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}

      - name: 🚀 Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          channelId: live
          projectId: evenpasssenegal
```

**Note** : J'ai mis directement `projectId: evenpasssenegal` en dur dans le workflow (plus simple).

---

### **Option 2 : Créer un nouveau projet Firebase `demdem`** (Propre mais plus long)

Si tu veux vraiment séparer les projets :

1. Crée un nouveau projet Firebase `demdem`
2. Récupère les nouvelles clés
3. Mets à jour tous tes secrets GitHub
4. Génère un nouveau `FIREBASE_SERVICE_ACCOUNT` pour le projet `demdem`

---

## 🚀 SOLUTION IMMÉDIATE (Option 1)

### Étape 1 : Ajoute les 2 secrets manquants

**GitHub → Settings → Secrets → New repository secret**
```
Secret 1:
Name: FIREBASE_PROJECT_ID
Value: evenpasssenegal

Secret 2:
Name: VITE_FIREBASE_MEASUREMENT_ID
Value: G-FVQTV8TMLJ
```

### Étape 2 : Vérifie tes secrets GitHub actuels

Assure-toi qu'ils correspondent **EXACTEMENT** à tes clés Firebase :
```
VITE_FIREBASE_API_KEY = AIzaSyDPsWVCA_Czs64wxiBOqUCSWwbkLMPNjJo
VITE_FIREBASE_AUTH_DOMAIN = evenpasssenegal.firebaseapp.com
VITE_FIREBASE_DATABASE_URL = https://evenpasssenegal-default-rtdb.europe-west1.firebasedatabase.app
VITE_FIREBASE_PROJECT_ID = evenpasssenegal
VITE_FIREBASE_STORAGE_BUCKET = evenpasssenegal.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID = 882782977052
VITE_FIREBASE_APP_ID = 1:882782977052:web:1f2ea147010066017cf3d9
VITE_FIREBASE_MEASUREMENT_ID = G-FVQTV8TMLJ
